(* From PSI:

def main(){
    a := uniform(0,1);
    b := uniform(0,a);
    c := uniform(0,b);
    d := uniform(0,c);
    return d; // expected: -1/6·[-1+d≤0]·[-d≤0]·[d≠0]·log(d)³
}

Fully determinizable and deterministic after determinization.
*)

let a = uniform(0,1) in 
let b = uniform(0,a) in
let c = uniform(0,b) in 
let d = uniform(0,c) in d