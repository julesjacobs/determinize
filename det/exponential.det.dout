== Elaboration ==
raw:
( let x =
    ( exponential( ( 2 : float[G] ) ) : float[G] )
    in
    ( let y = ( exponential( ( x : float[G] ) ) : float[?m3] ) in ( y : float[?m3] ) : float[?m3] ) : float[?m3] )
defaulted:
( let x =
    ( exponential( ( 2 : float[G] ) ) : float[G] )
    in
    ( let y = ( exponential( ( x : float[G] ) ) : float[E] ) in ( y : float[E] ) : float[E] ) : float[E] )

== Determinized ==
let x = exponential( 2 ) in let y = 1 / x in y

== Evaluation (100 trials) ==
program mean: 9.59522
determinized mean: 8.65512
